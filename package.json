{
  "name": "beacon-api",
  "version": "0.1.0",

  "scripts": {
    "start": "npx tsx src/server.ts",
    "test:observer": "npx tsx src/test-observer.ts"
  },

  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5"
  },

  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
